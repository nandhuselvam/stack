import { useState } from 'react';
import API from '../services/api'


const useApi = () => {
		const [res]
	const call = async () => {
	
		try {
			let response = await API();

		} catch (error) {}
	}
}