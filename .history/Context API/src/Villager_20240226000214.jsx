import React from 'react'

function Villager() {

	const {profileName, setProfileName} = useContext(profileNameContext);
	const [editMode, setEditMode] = const [state, setstate] = useState(initialState)
   return (
	<div>
	  
	</div>
  )
}

export default Villager
